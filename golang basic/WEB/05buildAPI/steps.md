1. in cmd -> go get -u github.com/gorilla/mux
2. slice for dummy database
3. create router using gorrila mux `routerName := mux.NewRouter()`
4. Router handles the function using `handleFunc("path", func).Method()`