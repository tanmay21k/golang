For using the mongo in go you need to install the drivers for mongoDb first, the drivers can be installed using `go get go.mongodb.org/mongo-driver/v2/mongo`


Keep the single go file in the root of the folder and then we can place the other go files inside the folders inside 

Decide the context you want to use https://pkg.go.dev/context